# Resume Builder V2.1B Acceptance Checklist

Date: 2026-07-10

V2.1B 继续执行 V2.1A 的数据隔离、多简历、一页 A4、实时草稿、备份和点击定位检查，并增加整套数据导入导出验证。

## Automated Verification

```bash
npm test
```

2026-07-10 最终结果：156 项通过，0 项失败、取消或跳过。其中核心与数据包 48 项、编辑器与 API 80 项、渲染与生成 28 项。

- [x] 全部自动化测试通过，无失败、取消或跳过。
- [x] 导出内容、manifest SHA-256 和排除目录有自动化覆盖。
- [x] 非法路径、损坏 manifest、ZIP 限制和无效数据包被拒绝。
- [x] token 过期/取消、原子切换、回滚、隔离和并发写锁有自动化覆盖。
- [x] 编辑器导出、未保存拦截、检查摘要、确认导入和重新加载有端到端覆盖。
- [x] 已覆盖已有写操作阻止导入提交，以及客户端断开后后台生成仍持有写入门禁。

## Temporary-Root Round Trip

- [x] 真实数据目录只作为只读导出源。
- [x] 导出包只提交到全新的临时 `dataRoot`，不替换真实数据。
- [x] `resumes.json`、全部导出 YAML、照片和备份的 SHA-256 一致。
- [x] 活动 ID、全部简历 ID 和名称列表一致。
- [x] 临时导入目录能通过 `validateDataRoot()`。
- [x] 全部导出文件在临时导入后逐一通过 SHA-256 核对。
- [x] 导入后的临时数据不包含 `output/`。
- [x] 临时目标原数据完整保留在 `.pre-import-*` 目录。

## Browser Verification

- [x] `数据管理` 位于左侧顶部简历选择器旁，菜单不挤压右侧编辑表单。
- [x] 导出前显示个人信息和未加密警告，下载文件名正确。
- [x] 导入检查展示文件、创建时间、版本、简历数量、活动简历和名称列表。
- [x] 有未保存草稿时阻止导入，并把用户引导到保存操作。
- [x] 确认导入后重新读取活动简历和草稿预览，正式 PDF 显示待生成。
- [x] 1440×900 和 1024×900 视口均无外层溢出，对话框操作区可见。
- [x] 两个视口均显示完整 A4 草稿，预览正文已加载。
- [x] 浏览器控制台无错误。

## Git And Privacy

- [x] `git diff --check` 无格式错误。
- [x] Git 未跟踪真实 `resumes.json`、`resumes/*`、个人照片或 `.env.local`。
- [x] 导出和临时导入不会在代码仓库中生成运行时数据。
- [x] ZIP 本身不会写入 Git 工作区。
- [x] 文档明确 ZIP 未加密，且 Gitee 历史脱敏仍属于迁移 GitHub 前的独立工作。

## New Computer Flow

- [x] 旧电脑可从编辑器导出整套 ZIP。
- [x] 新电脑完成代码克隆、`npm install` 和可选 `.env.local` 配置后可启动编辑器。
- [x] 新电脑可检查 ZIP 摘要并确认整套导入。
- [x] 导入前旧数据保留为同级 `.pre-import-*`，导入后可重新生成 PDF。
